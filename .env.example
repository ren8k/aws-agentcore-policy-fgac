# AgentCore Gateway Configuration
# CDK デプロイ後の出力値を設定してください

# Gateway 設定（CDK 出力から取得）
CLIENT_ID=<GatewayCognitoClientId の値>
GATEWAY_URL=<AgentCoreGatewayUrl の値>
COGNITO_DOMAIN=<GatewayCognitoDomain の値>
MCP_TARGET_NAME=<AgentCoreGatewayTargetName の値>

# AWS リージョン
REGION=us-east-1

# Bedrock モデル ID（fgac_demo.py で使用）
MODEL_ID=openai.gpt-oss-20b-1:0

# OAuth リダイレクト URI
REDIRECT_URI=http://localhost:8080/callback
LOGOUT_REDIRECT_URI=http://localhost:8080/logout

# システムプロンプト（fgac_demo.py で使用、複数行は二重引用符で囲む）
SYSTEM_PROMPT="以下のルールを厳守しなさい。
<rule>
- 不必要にツールは実行しないこと。特に、利用可能なツール以外で解決できなければ、要求を拒否すること。
- 簡潔に回答すること。
</rule>"
